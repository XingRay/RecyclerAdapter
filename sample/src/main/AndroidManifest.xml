<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xingray.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".page.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".page.SingleItemClickActivity" />
        <activity android:name=".page.ViewPager2TestActivity" />
        <activity android:name=".page.JavaTestActivity" />
        <activity android:name=".page.MultiSimpleActivity" />
        <activity android:name=".page.MultiItemClickActivity" />
        <activity android:name=".page.SingleSimpleActivity" />
        <activity android:name=".page.SingleItemSelectActivity" />
        <activity android:name=".page.MultiItemSelectActivity" />
        <activity android:name=".page.MultiLayoutSimpleActivity" />
    </application>

</manifest>